# 项目阶段路线图

- 适用范围：个人使用、多个邮箱账户、Web + 桌面端、GPT AI
- 计划基线：单人配合 Codex 开发
- 预计周期：剩余约 15–22 周，可用版本在第 8–12 周形成
- 开发策略：每个阶段都交付一条可实际使用的完整工作流
- 交互专项：右键菜单按 [`design/context-menu-plan.md`](design/context-menu-plan.md) 的对象矩阵和安全规则分阶段实施

## 总体阶段

| 阶段 | 目标 | 预计时间 | 主要交付物 |
|---|---|---:|---|
| P0 基础架构 | 建立稳定的多供应商边界 | 已完成 | MailProvider、注册表、统一路由、契约测试 |
| P1 账户与真实连接器 | 安全连接真实邮箱账户 | 2–3 周 | 数据库、OAuth、凭据存储、首个真实 Provider |
| P2 邮箱垂直链路 | 形成可持续使用的统一收件箱 | 3–4 周 | 增量同步、Inbox UI、线程阅读、发送与恢复 |
| P3 个人组织核心 | 连接任务、日历和笔记 | 3–4 周 | Today、Task、Calendar、Note、EntityLink |
| P4 AI 助手 | 让 AI 理解并操作四类数据 | 3–4 周 | AI Provider、AI Command、工具调用、每日简报、行动项工作流 |
| P5 桌面与本地体验 | 提供可靠桌面应用 | 2–3 周 | Tauri、通知、快捷键、有限缓存、自动更新 |
| P6 稳定化 | 达到长期个人使用标准 | 2–4 周 | 安全、备份、性能、异常恢复和连续使用验证 |

## P0：基础架构

状态：核心工作已完成。

- [x] 项目定位和 MVP 边界；
- [x] `MailProvider` 统一接口；
- [x] 邮件、日历、授权和同步领域类型；
- [x] Provider 能力声明；
- [x] Provider 注册表和账户路由；
- [x] 跨账户统一收件箱聚合；
- [x] 统一错误模型；
- [x] 内存 Provider 和基础测试；
- [x] 整理为正式应用目录结构；
- [x] 建立持续类型检查和测试命令。

完成门槛：任何真实邮箱连接器都可以只实现 Provider 接口，不向 UI 或业务层泄露供应商差异。

## P1：账户、数据库与真实 Provider

### 数据层

- [x] 建立 PostgreSQL 数据层和启动时迁移；
- [x] 定义单用户版 `Account`、`EncryptedCredential`、`SyncCursor` 和 `SyncRun`；
- [x] 定义邮件、线程和文件夹本地索引表；
- [x] 建立启动时幂等 Schema 迁移；
- [x] 使用 AES-256-GCM 加密邮箱凭据并将主密钥与数据库分离；
- [x] 增加本地、CalDAV、ICS 与 Exchange 日历索引表；
- [ ] 正式密钥轮换工具。

### 账户连接

- [ ] 单用户登录；
- [ ] OAuth 发起、回调、刷新和撤销；
- [ ] 每个邮箱账户独立授权；
- [x] 账户名称、颜色、同步状态和暂停/启用；
- [ ] 账户顺序调整；
- [x] 账户保存前的只读连接测试契约和测试按钮；
- [x] 测试成功后保存账户并触发首次同步；
- [x] 账户同步状态与最近错误持久化；
- [x] 账户管理列表、重新配置、暂停和删除；
- [ ] OAuth 账户重新授权提示。

### 首个真实 Provider

- [x] 实现通用 IMAP/SMTP Provider；
- [ ] 获取账户资料；
- [x] 读取 IMAP 文件夹和未读计数；
- [x] 获取邮件并提供兼容线程映射；
- [ ] 获取日历列表和事件（IMAP/SMTP 不提供，后续由其他 Provider 实现）；
- [x] 把认证、网络、取消和远端错误映射为统一错误；
- [x] 添加 Provider 会话、安全边界和能力契约测试。

完成门槛：可以连接至少两个真实账户，并显示各自的账户信息、文件夹和最近邮件；一个账户失败不会影响另一个账户。

## P2：统一邮箱垂直链路

### 同步引擎

- [x] 每账户独立首次同步，按快速/推荐/完整范围读取首批摘要；
- [x] 按 IMAP UID 后台连续增量同步；
- [ ] Webhook/推送通知接入；
- [x] 本地单进程每账户定时补偿同步；
- [x] IMAP UID、UIDVALIDITY 和历史回填位置持久化；
- [x] 账户、文件夹和邮件摘要幂等写入与重复保护；
- [x] 最近 UID 窗口的已读、星标和移动/删除状态补偿；
- [x] 每账户并发锁和指数退避；
- [x] 同步运行日志和账户错误状态；
- [x] 手动触发单账户同步；
- [x] 进程运行期间自动重试，应用重启后从持久化游标恢复；
- [ ] 在线多实例任务队列和分布式锁。

### 邮箱功能

- [x] 统一收件箱读取真实本地邮件索引；
- [x] 点击邮件后按需读取正文、清洗 HTML 并安全缓存；
- [x] 按账户、文件夹、未读、星标和关键词筛选；
- [x] 邮件线程阅读；
- [x] 邮件右键菜单及标记已读、星标和归档；
- [x] 邮件拖动或菜单移动到指定文件夹；
- [x] 撰写、回复和转发；
- [x] 回复时自动判断收件人和抄送人，发送前确认发件账户；
- [x] 附件按需读取、下载、草稿附件和正文内嵌图片；
- [x] 发送操作幂等保护。

### Web UI 骨架

- [x] Next.js 应用；
- [x] 应用侧栏和全局命令入口；
- [x] Inbox 三栏布局（静态数据交互基线）；
- [x] 账户颜色和来源标识；
- [x] 收件箱加载和空状态；
- [x] 建立共享 `ContextMenu`、`ContextTarget` 和命令注册表；
- [x] 邮件右键命令提供相同的三点菜单入口；
- [x] 邮件菜单支持菜单键、`Shift + F10`、焦点恢复和原生菜单保底；
- [x] 任务、日历和笔记命令提供右键、三点菜单或详情工具栏入口；
- [ ] 账户命令提供三点菜单入口；
- [ ] 完整同步进度、错误恢复状态和离线状态；
- [x] 邮件列表方向键、Enter、Delete、菜单键和 `Shift + F10`；
- [ ] 全模块完整键盘导航、屏幕阅读器和触屏无障碍验收。

完成门槛：连续同步真实账户 7 天，无严重漏信、重复邮件或错误发件身份；可以在统一收件箱完成阅读和回复。

## P3：任务、日历、笔记与 Today

### 任务

- [x] 本地任务表、来源引用表和日历时间块关联表；
- [x] 任务创建、编辑、完成和删除；
- [x] 重要性、自动/手动紧急性、截止时间和预计时长；
- [ ] 提醒和重复任务；
- [x] Tasks 内的 Today、Inbox 和四象限视图；
- [x] Upcoming、Waiting、Completed 和项目视图；
- [x] 任务可通过排期窗口创建本地日历时间块，使用预计时长并检测日程冲突；
- [x] 任务拖入日历进行时间安排，并支持拖动已安排时间块；
- [x] 任务行右键/三点菜单，支持完成、编辑、重要性、紧急性、等待和删除；
- [x] 任务卡片、编辑窗口和右键菜单可返回关联邮件，Inbox 按本地邮件 ID 精确定位；
- [x] 任务菜单提供“安排到日历”，任务卡显示已安排时间并可返回日程；
- [ ] 任务菜单补充其他来源关联、复制和清单操作。

### 日历

- [x] 本地日历与多个 CalDAV 账户只读聚合；
- [x] Outlook/Exchange 等 HTTPS ICS 链接只读订阅、测试、加密保存和手动同步；
- [ ] CalDAV 双向写入、冲突处理和后台定时同步；
- [x] 本地 `CalendarProvider`、日历/事件索引表和 REST API；
- [x] 正式周视图：小时轴、全天区域、按时间定位和重叠事件排列；
- [x] 月视图：六周网格、跨日事件展示和更多日程展开；
- [x] 周/月切换、周期导航和视图偏好记忆；
- [ ] 日视图；
- [x] 创建、修改和删除本地事件；
- [x] 日历颜色；
- [x] IANA 时区和全天事件；
- [ ] 重复事件；
- [x] 读取 CalDAV 展开的重复实例、会议链接和参与者；
- [ ] 创建和修改重复事件、会议链接和参与者。
- [x] 日历事件和空白时间段右键菜单；
- [ ] 日历源右键菜单。

### 笔记

- [x] Projects 与 Notes 本地数据模型、校验、仓库和 REST API；
- [x] 项目导航、笔记列表和无边框编辑器三栏界面；
- [x] 笔记创建、编辑、自动保存、删除、搜索、项目筛选和置顶；
- [x] Plate 53 + shadcn/Radix 编辑器接入正式 Notes 页面，使用版本化 Plate JSON；
- [x] Markdown 输入、`/` 内容块菜单、拖拽、浮动工具栏、表格和 DOCX 能力；
- [x] 移除旧编辑器实现，停用独立实验页并跳转至正式 Notes；
- [x] Plate 暂时关闭 AI 路由、密钥设置和全部 AI 触发入口；
- [ ] 完成 Plate 中文输入、长文、粘贴、移动端和键盘操作人工验收；
- [x] 项目组织和标题/正文搜索；
- [ ] 标签和数据库级全文索引；
- [ ] 会议笔记模板；
- [x] 从笔记创建任务，并从笔记打开任务、从任务返回原笔记；
- [x] 笔记列表右键/三点菜单；编辑器保留系统复制、粘贴和拼写检查菜单。

### 统一关系

- [x] 建立任务的邮件、日历和笔记来源引用基础结构；
- [x] 从邮件正文按钮或右键菜单创建任务并保留邮件来源；
- [x] 从任务返回关联邮件，并对已归档、删除或尚未同步的来源给出提示；
- [x] 任务与本地日程时间块双向关联，可从任一对象精确返回另一方；
- [x] 建立通用 `EntityLink` 数据表和项目对象基础；
- [x] 建立 `EntityLink` 仓库、校验与 REST API，并自动迁移任务来源和任务排期关系；
- [x] 让项目等剩余模块全面迁移到通用 `EntityLink`；
- [x] 任务、事件和笔记双向关联；
- [x] 邮件加入统一双向关联；
- [x] 笔记与任务双向关联；
- [x] 统一“相关内容”面板，并接入邮件、任务、日历和笔记详情；
- [x] 日历事件创建会议笔记、准备任务和跟进任务；
- [x] Today 页面整合当天日程、需推进任务和未读邮件，并支持直接完成任务。
- [ ] Today 卡片和相关内容卡片复用来源对象的右键命令。

完成门槛：可以从邮件创建任务、把任务安排进日历、为事件建立笔记，并从任意对象返回来源。

## P4：AI 工作台

详细架构、数据模型、安全边界和分阶段实施顺序见 [`architecture/ai-integration-plan.md`](architecture/ai-integration-plan.md)。

### AI 基础

- [x] 可配置 OpenAI-compatible API Provider；
- [x] 一个 Provider 管理多个模型；
- [x] 服务端加密 API Key 和连接测试；
- [x] 按功能键绑定主模型与备用模型；
- [x] 流式输出与失败前单次回退；
- [x] 本地管理对话、消息和运行记录，并纳入备份；
- [ ] 请求延迟、限流、错误和令牌统计；
- [ ] 只发送最少必要上下文。

### AI 工具

- [ ] 邮件搜索和线程读取；
- [ ] 邮件草稿和发送确认；
- [ ] 日历查询、创建和修改确认；
- [ ] 任务创建和更新；
- [ ] 笔记搜索、创建和更新；
- [ ] 对象关联；
- [ ] 工具调用日志、撤销和失败恢复。
- [ ] AI 建议卡片和工具执行记录复用统一上下文命令、确认与撤销机制。

### 核心 AI 工作流

- [x] 邮件总结；
- [x] 邮件提取行动项；
- [ ] 将 AI 行动项转换为可编辑、可确认的任务建议；
- [ ] 每日简报；
- [ ] 查找日历空档并安排任务；
- [ ] 会前准备摘要；
- [ ] 会议笔记提取任务；
- [x] 跟进邮件草稿，并支持将正文要求替换为 AI 生成内容；
- [ ] 每周复盘。

### AI 质量

- [ ] 建立至少 30 个个人真实场景评估；
- [ ] 检查工具参数和确认边界；
- [ ] 防止重复发送和重复创建；
- [ ] 显示答案的数据来源；
- [ ] 明确区分建议、草稿和已执行动作。

完成门槛：30 个评估场景达到可接受结果；发送邮件、修改日程和删除数据没有绕过确认的路径。

## P5：桌面应用与本地体验

- [ ] Tauri 桌面外壳；
- [ ] Windows 和 macOS 打包；
- [ ] 系统安全凭据存储；
- [ ] 桌面通知和深链接；
- [ ] `Ctrl/Cmd + K` 全局命令；
- [ ] 全局快速记录；
- [ ] 启动、托盘和后台同步策略；
- [ ] 自动更新；
- [ ] SQLite 有限缓存；
- [ ] 断网时可查看最近数据和创建本地草稿。
- [ ] 桌面端复用 Web 命令注册表，并区分对象右键菜单与系统托盘菜单。

完成门槛：桌面端与 Web 使用同一账户和数据；断网不丢失草稿，重新联网后可以安全恢复。

## P6：稳定性、安全和长期使用

- [ ] OAuth 令牌、API 密钥和日志安全审查；
- [x] 完整 ZIP 备份导出、检查、恢复和恢复前安全副本；
- [ ] 使用真实备份完成独立恢复演练；
- [ ] 数据库迁移回滚策略；
- [x] HTML 邮件安全渲染、CID 图片和远程图片按需显示；
- [x] 邮件附件列表和下载；
- [ ] 常见附件类型的安全预览；
- [ ] CSP、CSRF、XSS 和会话保护；
- [ ] 大邮箱性能测试；
- [ ] 时区和夏令时测试；
- [ ] 供应商限流和宕机演练；
- [ ] AI 成本上限和异常调用保护；
- [ ] 连续四周真实日常使用；
- [ ] 修复所有数据丢失和错误外部写入问题。
- [ ] 完成右键菜单的键盘、屏幕阅读器、触屏、跨账户部分失败和三端一致性测试。

完成门槛：连续四周作为主要个人工作台使用，没有严重数据丢失、重复发送或无法恢复的同步故障。

## 执行任务清单

- 更新时间：2026-07-23
- 唯一任务源：本节用于安排实际开发顺序；上方阶段清单继续记录完整产品范围。
- 排序原则：先处理数据安全和真实使用稳定性，再完成 AI 只读上下文与受控写入，最后扩展桌面端和高级检索。
- 完成要求：任务必须同时满足实现、自动化检查和对应验收标准后才能勾选。

### 已完成基线

- [x] `BASE-001` 完成 P0 基础架构、Provider 边界、注册表、统一路由和契约测试；
- [x] `BASE-002` 建立 Git 私有仓库、`main` 基线提交和安全忽略规则；
- [x] `BASE-003` 增加仓库 README、快速启动和安全说明；
- [x] `BASE-004` 完成当前核心包与 Web 应用类型检查；
- [x] `BASE-005` 完成现有 Provider、存储、AI、邮件、日历、任务、笔记、关联和搜索测试。

### M1：真实使用稳定性

目标：在继续扩大 AI 写入能力前，先证明当前数据链路可以可靠日常使用。

- [ ] `STAB-001` 使用两个真实邮箱账户连续同步至少七天，记录新增邮件、历史回填、重启恢复和错误状态；
- [ ] `STAB-002` 回归真实邮件线程、已读、星标、移动、归档、删除、发送、回复、转发、附件和 CID 内嵌图片；
- [ ] `STAB-003` 验证一个账户认证失败、超时或限流时不会阻塞其他账户；
- [x] `STAB-004` 为最近 UID 窗口之外的邮件增加周期性状态深度审计；
- [x] `STAB-005` 为邮件正文缓存增加过期、容量上限和安全清理策略；
- [x] `STAB-006` 统一同步进度、离线状态、可恢复错误和重新授权提示；
- [x] `STAB-007` 优化完整测试执行时间，或将 CI 超时配置调整到稳定可完成的范围；
- [ ] `STAB-008` 增加大邮箱、供应商限流、网络中断和应用重启演练。

完成标准：两个真实账户连续七天没有严重漏同步、重复对象、错误外部写入或跨账户阻塞。

### M2：备份、迁移与密钥安全

- [ ] `SAFE-001` 使用真实数据执行 ZIP 导出、清空测试环境、恢复和结果比对；
- [x] `SAFE-002` 为数据库迁移建立版本记录、失败处理和回滚方案；
- [ ] `SAFE-003` 实现主密钥轮换工具，并验证旧凭据重新加密；
- [ ] `SAFE-004` 审查 OAuth 令牌、邮箱密码、AI API Key 和日志脱敏边界；
- [ ] `SAFE-005` 增加备份损坏、错误主密钥和附件缺失的恢复测试。

完成标准：可以从独立备份恢复全部业务数据和加密配置，失败恢复不会破坏现有数据。

### M3：AI 阶段 C——只读上下文与来源

目标：让 AI 能回答真实跨模块问题，但不允许产生业务写入。

- [ ] `AI-C01` 建立统一 `ai-context-service` 和稳定的上下文对象格式；
- [ ] `AI-C02` 接入 Today、全局搜索、邮件线程、日历事件、任务、笔记和 EntityLink；
- [ ] `AI-C03` 实现邮件、日历、任务和笔记的严格只读工具；
- [ ] `AI-C04` 为回答增加来源卡片、对象类型、摘要和深链接；
- [ ] `AI-C05` 增加数据范围选择、单项上限、总 token 预算和内容裁剪；
- [ ] `AI-C06` 默认只发送完成请求所需的最少上下文；
- [ ] `AI-C07` 将邮件、笔记和外部内容视为不可信数据，增加 Prompt Injection 测试；
- [ ] `AI-C08` 记录模型、延迟、回退、429、错误、输入 token 和输出 token；
- [ ] `AI-C09` 增加只读工具绝不产生数据库或远端写入的集成测试。

完成标准：AI 可以回答真实的跨模块问题，用户能查看每条回答使用的数据来源，执行前后业务数据保持不变。

### M4：AI 阶段 D——操作预览与确认

目标：AI 只能提出结构化变更，未经用户确认不得执行。

- [ ] `AI-D01` 新增 `ai_action_proposals` 表、状态、过期时间、幂等键和审计字段；
- [ ] `AI-D02` 实现操作预览、修改、批准、拒绝和过期界面；
- [ ] `AI-D03` 第一批开放任务创建、任务更新、笔记创建和笔记更新；
- [ ] `AI-D04` 第二批开放日历事件建议和邮件草稿建议；
- [ ] `AI-D05` 复用现有输入验证、日历冲突检测、EntityLink 和来源回链；
- [ ] `AI-D06` 防止重复批准、页面重试或网络重放产生重复对象；
- [ ] `AI-D07` 增加工具调用日志、失败恢复和可撤销操作；
- [ ] `AI-D08` 明确区分建议、草稿、待确认和已执行动作；
- [ ] `AI-D09` 保持发送邮件、删除数据和批量操作为禁止或强制二次确认；
- [ ] `AI-D10` 增加批准前后数据库差异、幂等和安全门集成测试。

完成标准：未经确认数据库不变化，重复确认不会创建重复记录，所有执行结果均可追踪。

### M5：完整 AI 工作流

- [ ] `FLOW-001` 将邮件行动项转换为可编辑、可确认的任务建议；
- [ ] `FLOW-002` 生成带来源的 Today 每日简报；
- [ ] `FLOW-003` 查找日历空档并提出任务排期建议；
- [ ] `FLOW-004` 为复杂任务提供拆解、预计时长和下一步建议；
- [ ] `FLOW-005` 根据会议、相关邮件和笔记生成会前准备摘要；
- [ ] `FLOW-006` 从会议笔记提取任务并建立来源关系；
- [ ] `FLOW-007` 根据已完成事项生成跟进邮件草稿；
- [ ] `FLOW-008` 生成每周任务、时间、邮件和项目复盘；
- [ ] `FLOW-009` 将 Notes 编辑器和 AssistantPanel 接入真实后端及来源卡片；
- [ ] `FLOW-010` 建立至少 30 个中文、德语和英语真实场景评估。

### M6：任务、日历和笔记补齐

#### 项目管理

- [x] `PROJ-001` 让任务使用真实 Project 关联，继承项目领域与颜色，并迁移旧项目名称；
- [x] `PROJ-002` 建立独立 Projects 模块和项目主页，汇总任务进度、笔记与已安排时间；
- [x] `PROJ-003` 支持项目创建、编辑、归档、快速添加下一步行动和项目笔记；
- [x] `PROJ-004` 增加项目里程碑及轻量时间线；
- [x] `PROJ-005` 移除低频硬编码项目模板，项目统一从空白创建；
- [x] `PROJ-006` 增加项目周复盘、停滞项目提示和下一周行动整理。
- [x] `PROJ-007` 增加可编辑甘特图、任务计划日期、前置依赖和循环依赖保护。

#### 任务

- [ ] `TASK-001` 增加提醒；
- [ ] `TASK-002` 增加重复任务及重复实例生成规则；
- [ ] `TASK-003` 增加标签、清单、复制和其他来源关联命令。

#### 日历

- [ ] `CAL-001` 增加日视图；
- [ ] `CAL-002` 支持创建和修改重复事件；
- [ ] `CAL-003` 支持创建和修改参与者及会议链接；
- [ ] `CAL-004` 增加 CalDAV 后台定时同步；
- [ ] `CAL-005` 增加 CalDAV sync-token/CTag 增量同步；
- [ ] `CAL-006` 增加 CalDAV 双向写入、版本检查和冲突处理；
- [ ] `CAL-007` 增加时区、夏令时、全天和重复事件边界测试。

#### 笔记与关系

- [ ] `NOTE-001` 增加笔记标签和数据库级全文索引；
- [ ] `NOTE-002` 增加会议笔记模板；
- [ ] `NOTE-003` 完成 Plate 中文输入、长文、粘贴、移动端和键盘操作验收；
- [x] `LINK-001` 让项目等剩余模块全面迁移到通用 EntityLink；
- [ ] `LINK-002` 让 Today 和相关内容卡片复用来源对象命令。

### M7：公网部署安全门

以下任务在本机或可信私网试用时可以延后，但公网部署前必须完成。

- [ ] `WEB-SEC-001` 增加单用户登录、安全会话和注销；
- [ ] `WEB-SEC-002` 实现 OAuth 发起、回调、刷新、撤销和重新授权；
- [ ] `WEB-SEC-003` 增加 CSP、CSRF、XSS 和安全响应头；
- [ ] `WEB-SEC-004` 增加 API 限流、请求大小限制和异常调用保护；
- [ ] `WEB-SEC-005` 增加 AI 成本上限、频率限制和敏感上下文审计；
- [ ] `WEB-SEC-006` 迁移到托管 PostgreSQL，并建立在线备份和监控；
- [ ] `WEB-SEC-007` 为多实例同步增加任务队列和分布式锁。

完成标准：公网部署不依赖隐藏 URL 或可信网络，未登录用户无法访问任何个人数据和操作接口。

### M8：交互、无障碍与桌面端

- [ ] `UX-001` 补齐账户、日历源、附件、相关内容和 AI 建议的上下文菜单；
- [ ] `UX-002` 完成全模块键盘导航、焦点恢复和快捷键验收；
- [ ] `UX-003` 完成屏幕阅读器、触屏三点菜单和 iPhone 响应式验收；
- [ ] `UX-004` 增加常见附件类型的安全预览；
- [ ] `DESK-001` 建立 Tauri 桌面外壳；
- [ ] `DESK-002` 完成 Windows 和 macOS 打包；
- [ ] `DESK-003` 接入系统安全凭据、通知、深链接、托盘和后台同步；
- [ ] `DESK-004` 增加自动更新；
- [ ] `DESK-005` 增加 SQLite 有限缓存及断网查看和本地草稿。

### M9：后续增强

- [ ] `SEARCH-001` 配置 Embedding 模型和向量存储；
- [ ] `SEARCH-002` 增加内容切分、版本哈希和增量 Embedding；
- [ ] `SEARCH-003` 实现关键词与语义混合排序；
- [ ] `SEARCH-004` 增加相似内容和自动关联建议；
- [ ] `OPS-001` 完成连续四周真实日常使用并修复所有数据丢失和错误外部写入问题。

## 建议执行顺序

1. `STAB-001` 至 `STAB-007`，同时完成 `SAFE-001`；
2. `AI-C01` 至 `AI-C09`；
3. `AI-D01` 至 `AI-D10`，先开放任务和笔记；
4. `FLOW-001`、`FLOW-002` 和 `FLOW-003`；
5. 按真实使用反馈选择 M6 的任务、日历或笔记缺口；
6. 准备公网部署时执行 M7，准备桌面交付时执行 M8；
7. Embedding 和高级自动关联保持在 M9。
