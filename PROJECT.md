# 个人 AI 工作台项目说明

> 一个以 GPT 为统一操作入口，连接个人邮箱、日历、笔记和任务的在线与桌面工作台。

- 文档版本：0.1
- 更新日期：2026-07-20
- 当前阶段：方案设计 / MVP 规划
- 目标用户：个人使用

## 实施状态

- [x] 项目技术路线与 MVP 边界
- [x] 统一 `MailProvider` 适配层设计
- [x] 多账户 Provider 注册与路由
- [x] 跨账户统一收件箱聚合模型
- [x] 内存 Provider 与基础契约测试
- [ ] 第一个真实邮箱 Provider
- [ ] OAuth 凭据存储
- [ ] 邮件增量同步持久化
- [ ] GPT 邮件行动项工作流

项目执行计划见 [`docs/roadmap.md`](docs/roadmap.md)，AI 集成方案见 [`docs/architecture/ai-integration-plan.md`](docs/architecture/ai-integration-plan.md)，UI 设计规范见 [`docs/design/ui-style.md`](docs/design/ui-style.md)。

## 1. 项目定位

本项目不是复制 Outlook、Notion 或 TickTick 的全部功能，而是构建一个面向个人的 AI 工作台：

- 邮件负责输入与沟通；
- 任务负责执行；
- 日历负责时间安排；
- 笔记负责知识沉淀；
- GPT 负责理解、搜索、关联和协助操作。

项目的核心价值不在于把四个模块放进同一个应用，而在于建立连续工作流：

```text
邮件
  → GPT 识别行动项
  → 创建任务并保留原邮件链接
  → 查找日历空档并安排时间
  → 创建或关联工作笔记
  → 完成后生成跟进邮件草稿
```

## 2. 产品原则

1. **AI 优先，但用户掌控**：GPT 是统一入口，涉及外部写入和破坏性操作时必须确认。
2. **连接现有服务**：Gmail 或 Microsoft 365 继续作为邮件和日历的真实数据源。
3. **单用户优先**：首版不建设团队权限、多租户、计费和企业管理系统。
4. **在线优先，逐步离线**：先保证 Web 与桌面同步，后续增加本地缓存及离线编辑。
5. **按需提供上下文**：只把完成当前请求所需的数据发送给 GPT，不上传整个邮箱。
6. **关联而非复制**：邮件、事件、任务和笔记之间使用双向链接，避免产生失控副本。
7. **一个供应商起步**：首版只集成实际使用的一个邮箱体系。

## 3. 核心使用场景

### 3.1 今日工作台

首页统一展示：

- 今天的日程；
- 今天应完成的任务；
- 逾期任务；
- 需要回复的重要邮件；
- 最近会议产生的行动项；
- 最近编辑的相关笔记；
- GPT 生成的每日简报。

### 3.2 邮件转行动

- GPT 总结邮件线程；
- 识别待办、日期、联系人和项目；
- 一键创建任务并保留邮件反向链接；
- 根据空闲时间把任务安排进日历；
- 完成任务后生成回复邮件草稿。

### 3.3 会议工作流

- 日历事件关联准备任务和会议笔记；
- GPT 根据相关邮件和旧笔记生成会前摘要；
- 从会议笔记中提取行动项；
- 自动生成后续任务和跟进邮件草稿。

### 3.4 个人复盘

- 每日简报；
- 每周任务与时间使用总结；
- 未回复邮件和长期停滞事项检查；
- 根据邮件、任务、日历和笔记生成项目进展摘要。

### 3.5 自然语言操作示例

- “把张先生昨天的邮件变成周五前完成的任务。”
- “找一个本周两小时的空档安排这个任务。”
- “总结过去一周客户 A 的邮件、会议和笔记。”
- “根据今天下午的会议创建准备清单。”
- “把这篇笔记中的行动项提取为任务。”
- “根据会议记录起草跟进邮件。”
- “今天最重要的三件事是什么？”

## 4. 功能范围

### 4.1 MVP 必须包含

#### 邮箱

- OAuth 登录；
- 邮件列表和文件夹/标签；
- 阅读邮件线程；
- 撰写、回复和转发；
- 基础附件支持；
- 基础搜索；
- 邮件转任务；
- GPT 总结和回复草稿。

#### 日历

- 日、周、月视图；
- 创建、编辑和删除事件；
- 展示参与者和会议链接；
- 任务拖入日历进行时间安排；
- 事件关联笔记、任务和邮件。

#### 任务

- 标题、描述、状态和优先级；
- 截止时间和提醒；
- 重复任务；
- 标签或项目；
- 来源链接；
- 列表视图和 Today 视图。

#### 笔记

- Markdown 或块编辑；
- 文件夹、标签或项目组织；
- 全文搜索；
- 笔记内容转任务；
- 关联邮件、日程和任务；
- GPT 总结、改写和行动项提取。

#### AI

- 全局 AI 输入框；
- 跨模块问答和搜索；
- 每日简报；
- 邮件分类、总结和回复草稿；
- 任务拆解和日历规划；
- 会议准备及会后整理；
- 工具调用记录和操作确认。

#### 客户端

- Web 应用；
- Windows/macOS 桌面应用；
- 桌面通知；
- 单用户身份验证；
- 数据导出和备份。

### 4.2 首版明确不做

- 多用户和团队协作；
- 订阅计费；
- 企业权限、审计和管理后台；
- 自建邮件服务器；
- 多个邮件供应商同时支持；
- 通用 IMAP/CalDAV 全兼容；
- 手机原生应用；
- 完整离线邮箱；
- 团队聊天和视频会议；
- Notion 级通用数据库系统；
- 未经确认自动发送邮件；
- 企业级数据驻留和合规套件。

## 5. AI 方案

### 5.1 API 与模型

- AI 服务通过设置页配置，一个 API 提供商可以管理多个模型；
- 首版实现 OpenAI-compatible API，首发提供商为 KI Connect；
- 使用 Function Calling / Tool Calling 连接应用能力；
- 默认模型：`mistral-small-4-119b-2603`；
- 摘要、分类和故障回退：`gpt-oss-120b`；
- 业务功能通过稳定的功能键绑定主模型和备用模型，模型名称不散落在组件中；
- API Key 仅在服务端加密保存，不从浏览器随每次 AI 请求传输；
- 网页端“无限消息”不视为 API 无限额度，需通过真实调用记录限流、延迟和稳定性；
- 详细设计和实施顺序见 [`docs/architecture/ai-integration-plan.md`](docs/architecture/ai-integration-plan.md)。

### 5.2 AI 工具

首版计划提供以下受控工具：

```text
search_emails
read_email_thread
draft_email
send_email
list_calendar_events
create_calendar_event
update_calendar_event
delete_calendar_event
search_notes
create_note
update_note
create_task
update_task
schedule_task
link_items
get_daily_context
```

每个工具必须：

- 使用明确的结构化参数；
- 验证用户输入和模型参数；
- 返回稳定的结构化结果；
- 记录调用时间、结果和错误；
- 支持幂等或防止重复执行；
- 在需要时返回操作预览，等待用户确认。

### 5.3 操作权限

| 操作类别 | 默认策略 |
|---|---|
| 搜索、读取、总结 | 自动执行 |
| 起草邮件、生成计划 | 自动执行，不产生外部写入 |
| 创建任务和笔记 | 可自动执行，并提供撤销 |
| 更新任务和笔记 | 可执行，显示变更结果 |
| 创建或修改日程 | 执行前确认 |
| 发送邮件 | 必须确认 |
| 删除邮件、事件、任务或笔记 | 必须确认 |
| 批量操作 | 必须预览并确认 |

### 5.4 AI 上下文策略

- 先在本地数据库和供应商 API 中检索；
- 只向模型发送排名靠前且与请求相关的内容；
- 邮件正文优先截取相关线程或段落；
- 长期偏好存储为显式、可编辑的个人设置；
- 不依赖模型隐式记忆保存关键业务状态；
- 对发送给 AI 的数据提供可查看记录；
- 聊天记录和业务状态由应用数据库管理。

## 6. 数据模型

核心实体：

```text
User
Account
EmailThread
EmailMessage
CalendarEvent
Task
Note
Project
EntityLink
AiConversation
AiAction
SyncCursor
```

`EntityLink` 是有机结合的关键，用于表示：

- 邮件产生了哪个任务；
- 任务安排在哪个事件或时间块；
- 会议对应哪篇笔记；
- 笔记产生了哪些任务；
- 回复邮件对应哪个已完成事项；
- 多个对象属于哪个项目。

建议字段：

```text
EntityLink
- id
- source_type
- source_id
- target_type
- target_id
- relation_type
- created_at
- metadata
```

## 7. 技术架构

```text
Web 应用 ─────────────┐
                     ├─> 个人后端 API ─> PostgreSQL
Tauri 桌面应用 ───────┘          │
                                 ├─> Gmail API 或 Microsoft Graph
                                 ├─> 可配置 AI Provider API
                                 └─> 后台同步任务

桌面应用 ─> 系统通知 / 凭据存储 / 后期 SQLite 本地缓存
```

### 7.1 推荐技术栈

- 语言：TypeScript；
- Web：Next.js + React；
- 桌面：Tauri；
- UI：Tailwind CSS 或同类方案；
- 富文本：TipTap；
- 后端：Node.js 24 LTS（项目最低版本 24.18.0）+ TypeScript；
- 数据库：PostgreSQL；
- ORM：Prisma 或 Drizzle；
- 搜索：PostgreSQL 全文搜索，后期按需增加向量搜索；
- 后台任务：轻量队列或独立 Worker；
- 桌面缓存：后期加入 SQLite；
- 部署：个人 VPS 或托管平台；
- 监控：结构化日志、错误跟踪和同步状态页面。

### 7.2 部署形态

采用单用户“私人云优先”模式：

- 后端和数据库部署在个人可控环境；
- Web 客户端通过身份验证访问个人后端；
- Tauri 桌面端调用同一后端；
- 首版在线使用，桌面端提供通知和系统集成；
- 第二阶段加入 SQLite 缓存和有限离线能力；
- 不在桌面安装包中存放 AI API 密钥。

## 8. 同步策略

### 8.1 数据来源

- 邮件和日历以 Gmail 或 Microsoft 365 为真实数据源；
- 笔记、任务、关联关系和 AI 历史以本项目数据库为真实数据源；
- 外部数据只缓存必要的索引、摘要和标识符；
- 附件首版按需获取，不默认永久复制全部附件。

### 8.2 同步要求

- 使用供应商 OAuth；
- 支持增量同步和同步游标；
- 正确处理令牌过期和重新授权；
- 对 Webhook 丢失提供定期补偿同步；
- 保存供应商对象 ID 和版本信息；
- 防止重复邮件、事件和重复 AI 操作；
- 在 UI 中显示上次同步时间和错误状态。

## 9. 安全与隐私

- AI API 密钥仅在服务端使用主密钥加密保存，或由服务器环境变量提供；
- OAuth 访问令牌和刷新令牌加密保存；
- 桌面凭据使用操作系统安全存储；
- 全站 HTTPS；
- 数据库和备份加密；
- 提供手动备份、恢复和数据导出；
- 日志不得记录完整邮件正文、令牌或 API 密钥；
- 向 GPT 发送最少必要数据；
- 根据隐私需求为 Responses API 使用 `store: false`；
- 删除功能覆盖本地数据、缓存和可控的 AI 状态；
- 发送邮件和删除数据等高风险动作必须人工确认。

## 10. UI 信息架构

首版建议保留六个一级入口：

1. **Today**：日程、任务、重要邮件和每日简报；
2. **Inbox**：邮件列表、线程和 AI 辅助；
3. **Calendar**：事件与任务时间块；
4. **Tasks**：任务列表、项目和优先级；
5. **Notes**：笔记、会议记录和知识整理；
6. **AI Command**：跨模块自然语言操作。

全局交互：

- `Ctrl/Cmd + K`：全局搜索和命令；
- 全局快速记录；
- 所有实体提供“关联到……”入口；
- AI 操作显示数据来源和即将执行的动作；
- 写操作成功后提供明确反馈和撤销入口。

## 11. 开发路线

### 阶段 0：需求确认与原型（1–2 周）

- 确定首发邮箱供应商；
- 明确真实个人使用流程；
- 设计核心实体和关联模型；
- 完成 Today、Inbox 和 AI Command 原型；
- 定义 AI 工具权限和确认规则。

### 阶段 1：本地核心功能（2–4 周）

- 项目脚手架；
- 单用户身份验证；
- PostgreSQL 数据模型；
- 任务与笔记；
- 日历基础视图；
- EntityLink 关联能力；
- 全文搜索。

### 阶段 2：邮箱与日历集成（2–4 周）

- OAuth；
- 邮件增量同步；
- 日历同步；
- 阅读、回复和附件基础功能；
- 邮件转任务；
- 同步状态和错误恢复。

### 阶段 3：GPT 集成（2–3 周）

- Responses API；
- 模型路由；
- 受控工具调用；
- 每日简报；
- 邮件总结和回复草稿；
- 会议准备与行动项提取；
- 确认、审计和撤销机制。

### 阶段 4：Web 与桌面交付（1–2 周）

- Web 部署；
- Tauri 桌面打包；
- 系统通知；
- 自动更新策略；
- 数据备份和恢复。

### 阶段 5：稳定性完善（3–6 周）

- 同步压力和异常测试；
- 时区、夏令时及重复事件测试；
- 安全检查；
- 性能优化；
- AI 提示与工具评估；
- 成本和令牌使用监控；
- 日常使用反馈迭代。

## 12. 时间预估

在一个人配合 Codex、只支持一个邮箱供应商的前提下：

- 6–10 周：可以开始个人日常试用；
- 3–5 个月：较稳定的个人版本；
- 6 个月以上：多邮箱、离线、语音和高级自动化。

时间不包含企业合规、团队协作、移动原生应用和邮件托管。

## 13. 主要风险

1. **范围膨胀**：试图同时达到 Outlook、Notion 和 TickTick 的完整深度。
2. **同步可靠性**：漏同步、重复对象、令牌失效和 Webhook 丢失。
3. **AI 错误操作**：模型误解意图后发送邮件或修改日程。
4. **隐私泄露**：向模型发送了超过当前任务所需的数据。
5. **上下文成本**：把过多邮件和笔记放进提示导致延迟和费用上升。
6. **离线冲突**：多个客户端离线修改同一对象后的合并问题。
7. **平台限制**：邮箱供应商 API 权限、限流和 OAuth 配置变化。

缓解原则：首版单供应商、在线优先、最少上下文、操作确认、完整日志、持续备份。

## 14. MVP 完成标准

满足以下条件后，首版可视为完成：

- 可安全连接一个真实邮箱和日历账户；
- 邮件和事件能够稳定增量同步；
- 可以创建、编辑和搜索任务与笔记；
- 邮件、事件、任务和笔记能够双向关联；
- GPT 可以基于相关数据回答问题和生成草稿；
- GPT 可以通过工具创建任务和笔记；
- 发送邮件、修改日程和删除数据前有确认；
- Today 页面可以形成可用的每日工作视图；
- Web 和桌面端使用同一账户和数据；
- 数据可以备份和恢复；
- 连续日常使用两周没有严重数据丢失或重复问题。

## 15. 下一步决策

开始开发前需要确定：

1. 首发邮箱供应商：Gmail 或 Microsoft 365；
2. 首发桌面平台：Windows、macOS 或两者；
3. 后端部署位置：个人 VPS 或托管平台；
4. 是否接受首版在线优先、不支持完整离线；
5. 是否允许 AI 自动创建任务和笔记；
6. 每月可接受的 OpenAI API 与托管成本；
7. 是否需要语音输入和会议录音转写进入后续版本。

## 16. 一句话愿景

**邮件负责输入，任务负责执行，日历负责时间，笔记负责沉淀，GPT 负责理解和连接。**
